<li class="nav-item">
    <a class="nav-link" href="{{ path('panier') }}">Panier
        {% if menu == 0 %}

        {% elseif menu == 1 %}
            1
        {% else %}
            {{ menu }}
        {% endif %}
    <span class="sr-only">(current)</span></a>


</li>
