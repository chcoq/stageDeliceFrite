{% extends'navAdmin.html.twig' %}
{% block stylesheets %}

    <link rel="stylesheet" href="{{ asset('css/css/bootstrap-responsive.css') }}">
    <link rel="stylesheet" href="{{ asset('css/css/font-awesome.css') }}">
    <link rel="stylesheet" href="{{ asset('css/style.css') }}">
{% endblock %}
{% block title %}Category{% endblock %}

{% block cat %}
    <h1>Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>NameCat</th>
                <td>{{ category.nameCat }}</td>
            </tr>
        </tbody>
    </table>

    <a style="color: #343a40;font-weight: bold;"href="{{ path('category_index') }}">back to list</a>
 <br>
    <a style="color: #343a40;font-weight: bold;"href="{{ path('category_edit', {'id': category.id}) }}">edit</a>

    {{ include('category/_delete_form.html.twig') }}
{% endblock %}